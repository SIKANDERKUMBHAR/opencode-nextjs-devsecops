Place downloaded 3D assets here for source tracking.

This directory is optional. The live site reads models from `public/assets/models`.
Use `npm run models:download` to fetch models and `npm run models:optimize`
to create Draco-compressed versions in `public/assets/models/optimized`.
